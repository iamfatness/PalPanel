@* Rendered by per-server pages when the {Id} route doesn't resolve to a live runtime. *@
<div class="status-card warn">
    <h1>Server not found</h1>
    <div class="state">This server doesn't exist, or it's disabled.</div>
    <div class="actions"><a href="/">Back to fleet</a></div>
</div>
